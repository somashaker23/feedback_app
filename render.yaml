services:
  - type: web
    name: feedback
    runtime: python
    buildCommand: cd frontend && npm ci && npm run build
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        sync: false
