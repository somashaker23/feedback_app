[project]
name = "feedback"
version = "0.0.1"
description = "a feedback storage app"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.138.1",
    "uvicorn>=0.49.0",
    "asyncpg>=0.30",
    "sqlalchemy[asyncio]>=2.0",
    "python-dotenv>=1.0",
]
